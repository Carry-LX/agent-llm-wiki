2. 三层缓存体系 (Embedding / Retrieval / Answer)

这一点是很多在线 RAG 系统一定会做的:
第一层: Embedding 缓存
避免重复算向量。
第二层: 检索结果缓存
同样的 query，不需要每次都查向量库。
第三层: 答案缓存 (FAQ)
如果答案固定，那直接返回，甚至不需要走 RAG,
这三层缓存能把

© API 调用次数

。 Milvus 查询次数

。 LLM 调用次数
统统减少至少 30% ~ 60%,
