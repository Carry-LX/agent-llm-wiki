3. 缓存 (Embedding Cache) 一一把重复的工作彻底去掉

Embedding 最“浪费钱”的地方就是: 重复调用。
现实里你会遇到 :

。 用户各种用词相近的提问

。 FAQ 类问题

。 编写 RAG 项目时自己不断调试。 完全哈希匹配 or 近似>阔值相似
最佳策略: 把 query 一 vector 缓存在 Redis / KV 里。
缓存命中率甚至能达到 30 ~ 50%,
对于语料库 embedding，要提前离线算好，这样查询时就不需要临时生成embedding。
训练营里的实际项目中，把缓存引入后能把首字延迟直接砍掉 40% 以上。
